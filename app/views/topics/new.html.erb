<%= form_for @topic do |f|%>
    <div class="form-group">
        <%= f.label :title %>
        <%= f.text_field :title, class: 'form-control' %>
    </div>
    <div class="actions">
        <%= f.submit "Create", class: 'btn btn-primary' %>
    </div>
<% end %>